Use:
cargo run --release

Open the IP address shown in your terminal (e.g., http://192.168.1.50:5000) on any other device.


src/: Server and UI code.

create this: 
uploads/: Local file storage.

Cargo.toml: Build info.

First project with rust
